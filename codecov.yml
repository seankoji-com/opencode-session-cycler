comment:
  layout: "header, diff, flags, files"
  behavior: default
  require_changes: false

coverage:
  status:
    project:
      default:
        target: 90%
    patch:
      default:
        target: 90%

ignore:
  - "dist/**"
  - "**/*.test.ts"
